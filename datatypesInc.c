#include <stdio.h>

int main()
{
    int a; // 4 bits
    float b; // 4 bits
    double d; // 8 bits
    char c;   // 1 bits
    long int l; // 8 bits
    signed int s; // 4 bits positive integer
    unsigned int i; // 4 bits negative and positive number
    long double ld;  // 10 to 16 bits
    short int s; // 2 bits
    
    //datatypes in c
}
